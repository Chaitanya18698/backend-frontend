<div class="container">

  <p><button type="button" (click)="add()" class="btn btn-primary btn-sm" data-toggle="modal"
      data-target="#exampleModal">
      Add
    </button></p>

  <div>
    <table class="table table-bordered mt-2">
      <thead>
        <tr *ngIf="contentArray.length != 0">
          <th>Id</th>
          <th>Name</th>
          <th>Dob</th>
          <th>Email</th>
          <th>contact</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody  *ngIf="contentArray.length != 0">
        <tr  *ngFor='let item of contentArray;let  i = index'>
          <td>{{i+1}}</td>
          <td>{{item.Name}}</td>
          <td>{{item.Dob | date}}</td>
          <td>{{item.Email}}</td>
          <td>{{item.Contact}}</td>
          <td><button type="button" (click)="edit(item)" class="btn btn-primary btn-sm" data-toggle="modal"
              data-target="#exampleModal">
              Edit
            </button> <button type="button" (click)="delData(item.Id)" class="btn btn-danger btn-sm ml-1"
              data-toggle="modal" data-target="#exampleModal1">
              Delete
            </button></td>
        </tr>

      </tbody>
      <tfoot  *ngIf="contentArray.length == 0">
        NO Data Found
      </tfoot>
    </table>
  </div>

</div>
<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
  aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel"><span *ngIf="view">Add</span><span *ngIf="!view">Edit</span></h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <form>
          <div class="form-group">
            <label for="exampleInputEmail1">Name</label>
            <input type="email" [(ngModel)]="name" name="name" class="form-control" id="exampleInputEmail1"
              aria-describedby="emailHelp" placeholder="Enter Name">
          </div>
          <div class="form-group">
            <label for="exampleInputPassword1">DOB</label>
            <input type="date" [(ngModel)]="Date1 " name="Date1" class="form-control" id="exampleInputPassword1546"
              placeholder="Dateofbirth">
          </div>
          <div class="form-group">
            <label for="exampleInputPassword1">Email</label>
            <input type="text" [(ngModel)]="email" name="email" class="form-control" id="exampleInputPassword1"
              placeholder="Email">
          </div>
          <div class="form-group">
            <label for="exampleInputPassword1">Contact</label>
            <input type="text" [(ngModel)]="contact" name="contact" class="form-control" id="exampleInputPassword1"
              placeholder="phone number">
          </div>
        </form>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
        <button *ngIf="view" [disabled]="!name || !Date1 || !email || !contact" type="submit" (click)="addlist()"
          class="btn btn-success">Submit</button>
        <button *ngIf="!view" (click)='editlist()' type="submit" class="btn btn-success">Update</button>
      </div>
    </div>
  </div>
</div>
<div class="modal fade" id="exampleModal1" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
  aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        Are sure ?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
        <button type="submit" (click)="delete()" class="btn btn-success">confirm</button>

      </div>
    </div>
  </div>
</div>

<router-outlet></router-outlet>
